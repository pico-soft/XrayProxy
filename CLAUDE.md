# XrayProxy — SOCKS5/HTTP прокси для Android (Termux)
# Версия: 2.23-beta
# Перед любыми изменениями читай PROJECT_CONTEXT.md

# Правила разработки:
# - При изменении версии обновлять ВСЕ файлы
# - Flask: threaded=True
# - Тесты серверов через _run_temp_xray (временный порт)
# - Мониторинг не должен ломать текущее соединение
# - Не использовать .py отдельным токеном в bash-скриптах
# - Проверка: python3 -m py_compile *.py && bash -n *.sh
